# TAILOR configuration file (https://github.com/sleekbyte/tailor)
exclude:
 - Pods
 - tflApp Tests
only:
 - upper-camel-case
 - lower-camel-case
 - constant-naming
 - constant-k-prefix
# - max-file-length: 500
# - max-function-length
 - redundant-parentheses
 - terminating-semicolon
 - todo-syntax
 #- redundant-optional-binding
